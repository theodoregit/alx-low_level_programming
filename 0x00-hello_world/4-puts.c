#include <stdio.h>
/**
 * main - print a string of characters
 * Return: 0 and exit
 */
int main(void)
{
	char pro[] = "\"Programming is like building a multilingual puzzle";

	puts(pro);

	return (0);
}
